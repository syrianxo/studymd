"use client";

/**
 * LoginForm -- stub
 *
 * TODO (Workstream 3): implement this component.
 * See STUDYMD_V2_DEVELOPMENT_PLAN.md Workstream 3 for specification.
 */
export default function LoginForm() {
  return (
    <div
      style={{
        padding: "1.5rem",
        background: "var(--surface2)",
        border: "1px dashed var(--border)",
        borderRadius: "var(--radius-md)",
        color: "var(--text-muted)",
        fontFamily: "'Outfit', sans-serif",
        fontSize: "0.85rem",
        textAlign: "center",
        lineHeight: 1.6,
      }}
    >
      <strong style={{ color: "var(--text)", display: "block" }}>
        LoginForm
      </strong>
      Placeholder - implementation coming in Workstream 3.
    </div>
  );
}
